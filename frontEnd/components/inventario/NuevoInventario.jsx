import React from 'react'
let propTypes = React.propTypes

// Componentes
import InventarioForm from './InventarioForm.jsx'

export default NuevoInventario = (props)=>{
    return (
        <InventarioForm
            //inventario={null}
            clientes={this.props.clientes}
        />
    )
}
